import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
    container:{
        backgroundColor: '#A8D0E6',
        height: '75px',
        margin: 'auto',
        textAlign: 'center',
        userSelect: 'none',
    },
}))